# 格威博報堂集團 · 參與統計網站 — 進度說明

最後更新：2026-07-27

## 是什麼
兩頁式 Cloudflare Pages 網站，統計格威博報堂集團各子公司參加練息場活動。
- 線上網址（測試站）：https://attendance-j2e.pages.dev
  - `/all` = 全集團互動工具，**需密碼 `27390000`**（2026-07-27 加）
  - `/part` = **獨立對外版，不設密碼**：只有 UCG（拆七家子公司）＋先勢集團＋台北博報堂 HTM，
    含報到總表（可填員工總數算參與率）、Email 歸戶對照、各場四欄明細。
    **已拿掉連回 /all 的按鈕**，可單獨把網址給出去。
- 專案名（Cloudflare Pages）：`attendance-j2e`（2026-07-27 由 `attendance` 改名，舊網址 attendance-9yu 已停用）
- 雲端原始碼（**私有** repo）：https://github.com/jointoenjoy/attendance
  含姓名／完整 email 的原始匯出（`pipeline/_private/`）與金鑰已列入 `.gitignore`，不會上雲端。

## 資料來源（重要）
- 報名（正取／候補／請假）→ Wix 報名系統
  - Wix 狀態：ATTENDING=正取、IN_WAITLIST=候補、NOT_ATTENDING=請假
- 報到（實際到場）→ Google 現場報到表
- 去識別化：只輸出公司層級數字與 email @ 後網域，**絕不含姓名或完整信箱**。
  （2026-07-27 起連遮罩帳號也不再出現，歸戶一律照現有歸屬呈現。）

## 網頁數字 vs Google 試算表數字，為什麼不一樣（2026-07-27 查清）
1. **定義不同（主因）**。試算表「博報堂不重複參與人數計算」那張表算的不是報到：
   - 2025 欄＝拿各公司員工名冊比對（全員 727 − 未參與 546 ＝ 186），算「名冊上有幾個人至少來過一次」。
   - 2026 欄＝**報名**去重人數，**含請假、含候補、也含 7/30、7/31 尚未舉辦的場**。
   網頁總表算的是**當天實際報到**，2026 只算到 7/17 已辦完的場，所以一定比較小。
   2025 網頁 202 vs 試算表 186 的 16 人差距＝有來、但對不到公司名冊的人（離職／派遣／名字寫法不同／掛錯公司）。
2. **私人信箱被漏算**。7 筆（6 個 gmail、1 個 charismomo.com）試算表「所屬企業」欄有標，
   但網頁只看 @ 後網域就整筆丟掉。其中 @charismomo.com 那位 7/17 候補、當天有到 → 已修好，先勢 2026 報到 21→22。
3. **網域 vs 人工欄位打架**。@ua.com.tw／@unistyle.com.tw，網頁判 UCG、試算表標 HTM／PILOT。
   - @unistyle 已由大寶 2026-07-27 定案：**聯太（聯廣集團旗下）**，試算表標 PILOT 是錯的 → 已獨立成一列。
   - 其餘的，大寶 2026-07-27 決定**不再標「待確認」，一律照現有歸屬**（頁面已移除待確認區）。

⚠️ 手上的原始匯出 `_private/sheet_2026.txt` 是 **7/24** 的（2026 不重複＝175）；7/27 截圖是 163，
中間有人動過試算表。要完全對齊得重新匯出一次。

## 歸戶規則（大寶 2026-07-27 定）
- **網域合併**：現場是手 key 的，`.com.tw` / `.com` 與拼字常有誤 →
  **@ 後面的名字一樣就算同一家**，所有寫法變體先歸戶、再合併成一列呈現（`subsidiary_map.GROUPS`）。
  頁面上顯示合併後的名字（如 `@ua`），下方灰字列出實際出現過的所有寫法。
- **UCG 子公司**：聯廣、聯太、聯樂、艾斯、聯眾、2008傳媒、迪維、聯勤。
  - `unistyle` = **聯太**（聯廣集團旗下）。
  - `unisincere` = 聯勤（原始清單沒列，但資料裡有，先獨立一列）。
- **各場明細的報到與總表同源**：都來自 `part_summary.json` 的 `ev_att`，
  兩支腳本結尾都會自動對帳，加總對不起來會印 ❌。

## 員工總數與參與率（2026-07-27 新增，同日改為分年度）
- 總表的「員工總數」**2025 / 2026 各一欄**（兩年人數不同，分開填才算得準）。填了就即時算出：
  - **人次率** ＝ 該年人次 ÷ 該年員工總數（可能 >100%，代表平均每人參加不只一場）
  - **人數率** ＝ 該年人數 ÷ 該年員工總數（全公司有多少比例的人至少來過一次）
  - UCG 小計／三家合計的員工總數＝**已填**各列的加總（沒填的不算進去）。
- **存在雲端**：Cloudflare KV（沿用 /all 那顆 `STATE` binding，key `part-state-v1`），
  任何人開頁都讀得到最新值。資料長相：`{"headcount":{"2025":{公司:數},"2026":{公司:數}},"updated":...}`。
  端點 `site/functions/api/part-state.js`：
  - `GET /api/part-state` 公開可讀
  - `PUT /api/part-state` **公開可寫，已拿掉編輯碼**
    （2026-07-27 大寶決定：會進到這頁的人不多，讓每個人都能自己改）
  - 只收「公司名 → 非負整數」，其餘欄位一律丟掉；不存任何個資。
- 頁面操作：點「✎ 編輯員工總數」→ 填 2025／2026 的數字 → 「儲存到雲端」，不需要密碼。
  本機預覽（`python3 -m http.server`）沒有這個 API，會顯示「連不上雲端」，屬正常。

## 版面與設計（2026-07-27 調整）
- 三段小字都收進「**數據說明**」底線小字，點了跳彈窗（總表／歸戶對照／各場明細各一份）。
- 「Email 歸戶對照」與「各場明細」都變成**可收合大區塊**，標題右側有向下箭頭，**預設收合**。
- 各場明細展開後分成 **2026｜2025 兩塊**（各自可收合），每一場再各自展開看細項。
- 2025 各場的報到已補上（來自總表同一份名冊）；2025 現場名冊沒有 email，
  所以 2025 各場以**公司**為單位列，2026 仍以 **email 網域**為單位列。
- **改為練息場對外設計風格**（依 handbook『設計規範/練息場視覺系統.md』）：
  Logo 藍 `#004D89` 當標題色、白底＋粉彩區塊、圓角 16px、有機色塊。
- **套用 apple-design 動效**：彈簧曲線 `cubic-bezier(.32,.72,0,1)`、按下即縮放回饋、
  展開收合可中途打斷、只動 transform/opacity，並尊重系統「減少動態效果」設定。
- 大標題固定一行不斷行：「UCG · PILOT · HTM 練息場活動參與統計」（下方灰字說明已移除）。

## /all 管理端改版（2026-07-27 下午）
- `/all` 版面改成跟 `/part` 同一套：hero＋漂浮色塊、卡片、「數據說明」彈窗、大區塊可收合。
- **計算來源全部收進「數據說明」小字彈窗**，共 5 份：
  資料來源與原始數據（hero）／集團整體參與率（含兩條公式）／分組數據表／歸戶／資料稽核。
- **原始數據可下載**：新增 `pipeline/build_all_downloads.py`，把三組原始檔輸出到 `site/all/data/`
  （9 個檔約 215KB，CSV 帶 BOM 讓 Excel 不亂碼）：
  - Wix 報名原始數據（2025／2026 逐場 × email 網域）
  - Wix 報名狀態（正取／候補／請假）
  - 併檔後：報名 ＋ 現場報到、各公司兩年度彙總
  下載連結就放在「數據說明」彈窗裡，依段落只列相關的那幾檔。
- **個資防呆**：`build_all_downloads.py` 寫檔前用正則掃「完整信箱」，抓到就整支中止、不留半成品。
  已驗證 10 個輸出檔全部 0 命中——只有 `@ 之後的網域`與計數，沒有姓名、沒有完整信箱。
- **不需要改 middleware**：`_middleware.js` 的 `GATED` 規則本來就含 `startsWith("/all/")`，
  所以 `/all/data/*` 一併被密碼擋住（已實測未帶 cookie 回 401、帶對的 cookie 回 200）。
- **Google 現場報到表連結**放在同一個彈窗，預設指向 Drive 的「2026_Chill療癒工作坊總表」，
  可在頁面上直接貼別份網址覆蓋，存進 KV（`S.sheetUrl`）跟員工總數同一份，不用改後端。
  這份表含姓名與完整信箱，所以**只放連結、不放檔案**。
- **員工總數可隨時修改、改完即時存雲端**（0.35 秒防抖後 PUT），右上角徽章顯示儲存狀態。
- **只做淺色模式**（2026-07-27 大寶決定），已移除深色 token 與主題切換鈕。
- **資料稽核分成 2026／2025 兩塊**（2026-07-27 晚間）：各自可收合，各自有「全年網域總表」＋逐場明細，
  標題列出當年場次數、有效場數、報名人次、網域種數（跟 `/part` 的「各場明細」分年寫法一致）。

## 檔案結構
- `site/` = 部署的網站原始檔（index / all / part / functions / wrangler.toml）
- `pipeline/` = 抓 Wix、併 Google Sheet 的 Python 腳本與中繼資料
  - `subsidiary_map.py` **歸戶單一真相**：email 網域 → 子公司、2025 名冊 label → 子公司
  - `pull_states.py` 抓 Wix 每場每網域正取/候補/請假 → wix_states.json（要 keys.env）
  - `parse_part.py` 讀 `_private/sheet_2026.txt` → part_summary.json（去識別化）
  - `merge_part_events.py` 併 Wix ＋報到，只留三家 → part_events.json
  - `build_part.py` 產出 site/part/index.html
  - `merge_states.py` ＋ `attendance_data.json` 供 /all 用，維持原樣
  - `build_all_downloads.py` 產出 `site/all/data/`（/all 頁可下載的原始檔＋manifest.json）
- `site/all/data/` = 給管理端下載的去識別化原始檔，跟著 git 走（已確認無個資），
  外面的人拿不到，因為整個 `/all/` 有密碼
- `pipeline/_private/` = 含姓名與完整 email 的原始匯出，**已列入 .gitignore，不進 git**
- ⚠️ keys.env（Wix 金鑰）**沒有**複製進來，只在原本暫存區。

## 重跑 /part 的指令
```
cd pipeline
python3 parse_part.py && python3 merge_part_events.py && python3 build_part.py
```

## 重跑 /all 原始數據下載檔的指令
```
cd pipeline && python3 build_all_downloads.py
```
（資料有更新時要重跑，否則 `/all` 彈窗裡下載到的還是舊檔）

## 部署指令
```
cd site && npm_config_cache=/tmp/afu-npm-cache2 npx wrangler@4 pages deploy . --project-name attendance-j2e --commit-dirty=true
```

## 密碼（2026-07-27 加）
- `/all`（含資料端點 `/api/state`）需密碼 **`27390000`**，輸對後同一台裝置 30 天內不用再輸。
- `/part` 與 `/api/part-state` **完全不設密碼**，維持可以獨立把網址給外部。
- 實作在 `site/functions/_middleware.js`；要換密碼不用改程式，
  到 Cloudflare Pages 專案設環境變數 `ALL_PASSWORD` 即可。

## 待辦／未決
- [ ] 撤銷 Wix API 金鑰（用完了，安全起見）
- [ ] 「聯勤」是否要獨立一列，還是併回聯廣（目前獨立一列，大寶尚未回覆）
- [ ] 舊的 Cloudflare Pages 專案 `attendance`（attendance-9yu.pages.dev）還留著，要不要刪要大寶點頭
- [x] 2026-07-27 已建 git 並推上 GitHub 私有 repo（jointoenjoy/attendance）
- [x] 2026-07-27 /all 已加密碼、/part 維持公開；網址改為 attendance-j2e
- [x] 2026-07-27 歸戶待確認區已移除，一律照現有歸屬
