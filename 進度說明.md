# 格威博報堂集團 · 參與統計網站 — 進度說明

最後更新：2026-07-27

## 是什麼
兩頁式 Cloudflare Pages 網站，統計格威博報堂集團各子公司參加練息場活動。
- 線上網址（測試站）：https://attendance-9yu.pages.dev
  - `/all` = 全集團互動工具
  - `/part` = **獨立對外版**：只有 UCG（拆七家子公司）＋先勢集團＋台北博報堂 HTM，
    含報到總表（可填員工總數算參與率）、Email 歸戶對照、各場四欄明細。
    **已拿掉連回 /all 的按鈕**，可單獨把網址給出去。
- 專案名（Cloudflare Pages）：`attendance`（因 attendance 已被占用，網址帶 -9yu）

## 資料來源（重要）
- 報名（正取／候補／請假）→ Wix 報名系統
  - Wix 狀態：ATTENDING=正取、IN_WAITLIST=候補、NOT_ATTENDING=請假
- 報到（實際到場）→ Google 現場報到表
- 去識別化：只輸出公司層級數字與 email @ 後網域，絕不含姓名或完整信箱。
  唯一例外：歸戶「待確認」清單顯示遮罩後的帳號前兩碼（如 `gr***@ua.com.tw`），讓對方能指認是誰。

## 網頁數字 vs Google 試算表數字，為什麼不一樣（2026-07-27 查清）
1. **定義不同（主因）**。試算表「博報堂不重複參與人數計算」那張表算的不是報到：
   - 2025 欄＝拿各公司員工名冊比對（全員 727 − 未參與 546 ＝ 186），算「名冊上有幾個人至少來過一次」。
   - 2026 欄＝**報名**去重人數，**含請假、含候補、也含 7/30、7/31 尚未舉辦的場**。
   網頁總表算的是**當天實際報到**，2026 只算到 7/17 已辦完的場，所以一定比較小。
   2025 網頁 202 vs 試算表 186 的 16 人差距＝有來、但對不到公司名冊的人（離職／派遣／名字寫法不同／掛錯公司）。
2. **私人信箱被漏算**。7 筆（6 個 gmail、1 個 charismomo.com）試算表「所屬企業」欄有標，
   但網頁只看 @ 後網域就整筆丟掉。其中 @charismomo.com 那位 7/17 候補、當天有到 → 已修好，先勢 2026 報到 21→22。
3. **網域 vs 人工欄位打架**。@ua.com.tw／@unistyle.com.tw，網頁判 UCG、試算表標 HTM／PILOT。
   - @unistyle 已由大寶 2026-07-27 定案：**聯太（聯廣集團旗下）**，試算表標 PILOT 是錯的 → 已獨立成一列。
   - 其餘 3 筆列在 /part 的「Email 歸戶對照」待確認區，等對方回覆再定案。

⚠️ 手上的原始匯出 `_private/sheet_2026.txt` 是 **7/24** 的（2026 不重複＝175）；7/27 截圖是 163，
中間有人動過試算表。要完全對齊得重新匯出一次。

## 歸戶規則（大寶 2026-07-27 定）
- **網域合併**：現場是手 key 的，`.com.tw` / `.com` 與拼字常有誤 →
  **@ 後面的名字一樣就算同一家**，所有寫法變體先歸戶、再合併成一列呈現（`subsidiary_map.GROUPS`）。
  頁面上顯示合併後的名字（如 `@ua`），下方灰字列出實際出現過的所有寫法。
- **UCG 子公司**：聯廣、聯太、聯樂、艾斯、聯眾、2008傳媒、迪維、聯勤。
  - `unistyle` = **聯太**（聯廣集團旗下）。
  - `unisincere` = 聯勤（原始清單沒列，但資料裡有，先獨立一列）。
- **各場明細的報到與總表同源**：都來自 `part_summary.json` 的 `ev_att`，
  兩支腳本結尾都會自動對帳，加總對不起來會印 ❌。

## 員工總數與參與率（2026-07-27 新增）
- 總表多一欄「員工總數」。填了就即時算出：
  - **人次率** ＝ 人次 ÷ 員工總數（可能 >100%，代表平均每人參加不只一場）
  - **人數率** ＝ 人數 ÷ 員工總數（全公司有多少比例的人至少來過一次）
  - UCG 小計／三家合計的員工總數＝**已填**各列的加總（沒填的不算進去）。
- **存在雲端**：Cloudflare KV（沿用 /all 那顆 `STATE` binding，新 key `part-state-v1`），
  任何人開頁都讀得到最新值。端點 `site/functions/api/part-state.js`：
  - `GET /api/part-state` 公開可讀
  - `PUT /api/part-state` 需 header `x-edit-code`，預設編輯碼 **`jte-ucg-2026`**
    （要換碼不用改程式：到 Cloudflare Pages 專案設環境變數 `PART_EDIT_CODE`）
  - 只收「公司名 → 非負整數」，其餘欄位一律丟掉；不存任何個資。
- 頁面操作：點「✎ 編輯員工總數」→ 輸入數字 → 填編輯碼 → 「儲存到雲端」。
  本機預覽（`python3 -m http.server`）沒有這個 API，會顯示「連不上雲端」，屬正常。

## 版面（2026-07-27 調整）
- 三段小字都收進「**數據說明**」底線小字，點了跳彈窗（總表／歸戶對照／各場明細各一份）。
- 「Email 歸戶對照」與「各場明細」都變成**可收合大區塊**，標題右側有向下箭頭，**預設收合**。
- 各場明細展開後分成 **2026｜2025 兩塊**（各自可收合），每一場再各自展開看細項。
- 2025 各場的報到已補上（來自總表同一份名冊）；2025 現場名冊沒有 email，
  所以 2025 各場以**公司**為單位列，2026 仍以 **email 網域**為單位列。

## 檔案結構
- `site/` = 部署的網站原始檔（index / all / part / functions / wrangler.toml）
- `pipeline/` = 抓 Wix、併 Google Sheet 的 Python 腳本與中繼資料
  - `subsidiary_map.py` **歸戶單一真相**：email 網域 → 子公司、2025 名冊 label → 子公司
  - `pull_states.py` 抓 Wix 每場每網域正取/候補/請假 → wix_states.json（要 keys.env）
  - `parse_part.py` 讀 `_private/sheet_2026.txt` → part_summary.json（去識別化）
  - `merge_part_events.py` 併 Wix ＋報到，只留三家 → part_events.json
  - `build_part.py` 產出 site/part/index.html
  - `merge_states.py` ＋ `attendance_data.json` 供 /all 用，維持原樣
- `pipeline/_private/` = 含姓名與完整 email 的原始匯出，**已列入 .gitignore，不進 git**
- ⚠️ keys.env（Wix 金鑰）**沒有**複製進來，只在原本暫存區。

## 重跑 /part 的指令
```
cd pipeline
python3 parse_part.py && python3 merge_part_events.py && python3 build_part.py
```

## 部署指令
```
cd site && npm_config_cache=/tmp/afu-npm-cache npx wrangler@4 pages deploy . --project-name attendance --commit-dirty=true
```

## 待辦／未決
- [ ] 撤銷 Wix API 金鑰（用完了，安全起見）
- [ ] 歸戶 3 筆待確認，等 UCG／HTM／先勢回覆（見 /part 的橘色框）
- [ ] 「聯勤」是否要獨立一列，還是併回聯廣（目前獨立一列，大寶尚未回覆）
- [ ] 待確認區要不要顯示遮罩帳號（目前顯示 `gr***@`）
- [ ] /part 已拿掉連回 /all 的按鈕，但 /all 網址本身還開著；要不要一起關掉
- [ ] 目前 attendance 資料夾還沒建 git／上 GitHub
